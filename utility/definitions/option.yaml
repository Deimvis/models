x-cg-header-default_volume-dst: ../{{ output_type }}/dmutil

components:
  schemas:

    Option:
      type: object
      additionalProperties: false
      properties:
        _enabled:
          type: boolean
          x-cg-go-field-name: Enabled
          x-cg-go-extra-tags: 'yaml:"_enabled"'

    ConstOption:
      type: object
      additionalProperties: false
      properties:
        _enabled:
          type: boolean
          x-cg-go-field-name: Enabled
          x-cg-go-extra-tags: 'yaml:"_enabled"'
